name: "DeleteUser"
display_name: "Delete User"
description: "Delete a user in AWS SSO."
enabled: true
collection_name: "aws-sso"
connection_types:
  aws-sso:
    name: aws-sso
parameters:
  "Email":
    display_name: Email
    type: "string"
    description: "The email of the user."
    required: true
    index: 1
